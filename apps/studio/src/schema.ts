// The VideoInput contract lives in @versus-engine/shared (packages/shared/src/video-input.ts)
// so the comparison builder and this renderer validate against the exact same schema.
export * from "@versus-engine/shared";
